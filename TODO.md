* create  images with only python and ansible installed (pip3/venv);
  this will allow reuse of ansible via multilayer

* create image variants without ansible and other cloud tools (az, aws, ...)
  they can either be the non-suffix versions, or they could be a completelly different image